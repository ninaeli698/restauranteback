services:
  - type: web
    name: restaurante-eli-backend
    runtime: python
    buildCommand: "pip install -r requirements.txt && python manage.py collectstatic --noinput"
    startCommand: "gunicorn menu_backend.wsgi:application"
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: restaurante-eli-db
          property: connectionString
      - key: SECRET_KEY
        generateValue: true
      - key: DEBUG
        value: "False"